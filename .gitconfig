


[user]
	name = anmolgupta1005
	email = anmol.gupta1005@gmail.com
[core]
	excludesfile = /home/anmol/.gitignore_global
	editor = subl
[alias]
    add = add
    c = checkout
    cm = commit -m
    b = branch
    pull = pull
    push = push 
    log = log --graph --abbrev-commit --decorate --date=relative --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)' --all
    bare = config --bool core.bare true
    send = !git add . &&  git commit -m \"AUTOCOMMIT `whoami`@`hostname`:`date`\" && git push origin master
[web]
    browser = firefox
[instaweb]
    httpd = apache2 -f
